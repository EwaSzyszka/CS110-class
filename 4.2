#HIRE PROBLEM

import numpy as np

def hire_assistant(current_assistant):
    Number_hired = 0 
    
    while np.random.uniform(0,1) > 1.1*(current_assistant):
        current_assistant = np.random.uniform(0,1)
        print ("New hire",current_assistant)
        Number_hired +=1
    else:
        print ("Old assistant ",current_assistant)
    print (Number_hired)

hire_assistant(0.1)

#HAT CHECKER

collected=[]
away = []

def hat_check(number_of_people):
    correctly_given_away = 0
    for i in range(number_of_people): 
        collected.append(np.random.randint(0,number_of_people))
    for i in range(number_of_people): 
        away.append(np.random.randint(2,number_of_people))   
    print away, collected
    for i in away:
        if away[i] == collected[i]:
            correctly_given_away +=1
    print ("Correct hat give away ",correctly_given_away)

hat_check(10)
